export interface Input {
  color: string;
  fields: string[];
  ignoreFailures: boolean;
  ignoreMessageNotFound: boolean;
  port: number;
  portRetries: number;
  status: string;
  text: string;
  timestamp: string;
}
